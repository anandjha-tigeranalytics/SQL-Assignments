1.Map the Lineage for all visuals with Tables names, Joins between associated tables (if more than one table used in visual), Column names and Other conditions (if applicable)
2.Write the Validation SQL for all the visuals in all tabs
3.Debug the issue wherever the visual shows mismatch between your query and the report data and describe the root cause for the same.